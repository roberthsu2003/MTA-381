# 流程控制
```
下列何者為s 介於50~59(不包含50和59)之間的條件式？
① s<60 and s>49    ② s>50 and s<59    ③ s>50 or s<59    ④ s>=50 or s<=59
```

```
15 <= age < 65的 if 結構條件式應如何撰寫？
① if (15 <= age < 65) :   ② if (age >= 15 and age < 65) :   ③ if (age >= 15 or age < 65) :  ④ if (age >= 15; age < 65) :

```

```
a=20  b=30 則執行 (a<=b) 運算式，其結果會傳回？
① True    ② False    ③ 1    ④ 0
```

```
優待票的年齡age，低於15或65以上的歲數，其條件式的寫法為何？
① age < 15 or age >= 65    ② age < 15 and age >= 65     ③ age >= 15 or age < 65    ④ age >= 15 and age < 65
```

```
執行下列程式區段，a值的結果為何？
a=8
b=0
if (a >= 6) : b = 9
if (b < 6) : a =-2

① 9    ② 6    ③ 8    ④ -2
```

```
下列何者不是Python選擇結構的敘述？
① if   ② if  else    ③ if  elif  else    ④ switch
```

```
試寫出下列運算式的比較結果？
① 12 > 12
② 20+12 == 40-8
③ 45 != 45
# 大於,小於要相同資料類型比對
# 等於可以不同資料類型,str > int
④ '30' < 60 
⑤ '0' == 48
⑥ 'A' == 65
```

```
① not(12 + 3 < 12 * 3)
② 20 > 12 or 40 < 8
③ (20 > 12) or (40 < 8)
④ 30 < 60 and 'A' != 'B'
⑤(20 > 13) or ('A' == 'B') and (10 >= 5)
⑥ 20 > 12 and not(40 < 8) 
```

```
3.處理10歲(含)以上，18歲(不含)以下的age年齡資料，條件式如何寫？
```
